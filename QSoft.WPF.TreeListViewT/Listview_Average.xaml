<UserControl Style="{DynamicResource UserControlStyle1}" x:Class="QSoft.WPF.TreeListViewT.Listview_Average"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QSoft.WPF.TreeListViewT"
             mc:Ignorable="d" 
             x:Name="local"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="HeaderArrowStyle" TargetType="Path">
            <Setter Property="Fill" Value="Gray"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
        </Style>

        <!--<Style TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                        <Grid>
                            <Border BorderThickness="0,0,1,1" BorderBrush="LightGray" Background="Transparent">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
                                    <ContentPresenter Content="{TemplateBinding Content}"/>
                                    <Path x:Name="SortArrowUp" Style="{StaticResource HeaderArrowStyle}" 
                                      Data="M 0 4 L 3.5 0 L 7 4 Z" Visibility="Collapsed"/>
                                    <Path x:Name="SortArrowDown" Style="{StaticResource HeaderArrowStyle}" 
                                      Data="M 0 0 L 3.5 4 L 7 0 Z" Visibility="Collapsed"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Tag" Value="Ascending">
                                <Setter TargetName="SortArrowUp" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="Descending">
                                <Setter TargetName="SortArrowDown" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>-->

    </UserControl.Resources>
    <Grid>
        <ListView ItemsSource="{Binding Datas}" SizeChanged="listview_SizeChanged" >
            <ListView.View>
                <GridView x:Name="gridview">
                    <GridViewColumn DisplayMemberBinding="{Binding Name}" Width="200">
                        <GridViewColumn.Header >
                            <GridViewColumnHeader Command="{Binding DataContext.SortCommand, ElementName=local}" CommandParameter="Name" >Name</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Age}" Width="100">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Command="{Binding DataContext.SortCommand, ElementName=local}" CommandParameter="Age">Age</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Time}" Width="150">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Command="{Binding DataContext.SortCommand, ElementName=local}" CommandParameter="Time">Time</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Width="100" Command="{Binding DataContext.DeleteCommand, ElementName=local}">Delete</Button>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
