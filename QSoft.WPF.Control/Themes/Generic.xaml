<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:QSoft.WPF.Control">


    <Style TargetType="{x:Type local:RadioButtonList1}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:RadioButtonList1}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ItemsPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="{x:Type RadioButton}">
                    <Setter Property="IsChecked" Value="{Binding IsSelected, RelativeSource={RelativeSource Self}, Mode=TwoWay}" />

                    <Setter Property="Content" Value="{Binding}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:RadioButtonList1}}, Path=DisplayMemberPath}" Value="{x:Null}">
                            <Setter Property="Content" Value="{Binding}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:RadioButtonList1}}, Path=DisplayMemberPath}" Value="">
                            <Setter Property="Content" Value="{Binding}" />
                        </DataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:RadioButtonList1}}, Path=DisplayMemberPath, TargetNullValue=''}" Value="" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.Setters>
                            </MultiDataTrigger.Setters>
                        </MultiDataTrigger>
                    </Style.Triggers>

                    <!--<Setter Property="Command" Value="{Binding DataContext.Command, RelativeSource={RelativeSource AncestorType={x:Type local:RadioButtonList1}}}" />
                    <Setter Property="CommandParameter" Value="{Binding}" />-->
                </Style>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
